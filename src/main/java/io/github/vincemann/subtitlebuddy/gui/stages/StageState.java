package io.github.vincemann.subtitlebuddy.gui.stages;

/**
 * Each stage has a state, which is one of the following.
 */
public enum StageState {
    OPEN,
    CLOSED,
    UNINITIALIZED,
    INITIALIZED;
}
